/* ============================================================
   ATLAS — templates.css
   Stage 4: Page Template Structural Styles
   Extends style.css (Stage 3). Must be loaded AFTER style.css.
   All tokens reference Stage 2 locked design system.
   ============================================================ */

/* ── Entity page shell layout ────────────────────────────── */
.entity-page {
  display: grid;
  grid-template-columns: 1fr 300px;
  gap: var(--space-8);
  align-items: start;
  padding-top: var(--space-8);
  padding-bottom: var(--space-16);
}

.entity-page__main {
  min-width: 0;
}

.entity-page__sidebar {
  position: sticky;
  top: calc(var(--nav-height) + var(--space-4));
  max-height: calc(100vh - 80px);
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: var(--space-5);
}

/* ── Entity header block ─────────────────────────────────── */
.entity-header {
  padding-bottom: var(--space-6);
  border-bottom: 1px solid var(--border);
  margin-bottom: var(--space-6);
}

.entity-header__type-row {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  margin-bottom: var(--space-2);
}

.entity-header__type {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--muted);
}

.entity-header__id {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--muted);
  background: var(--parchment);
  border: 1px solid var(--border);
  padding: 2px 8px;
}

.entity-header__title {
  font-family: var(--font-display);
  font-size: var(--text-4xl);
  font-weight: 300;
  line-height: 1.05;
  letter-spacing: -0.02em;
  color: var(--ink);
  margin-bottom: var(--space-3);
}

.entity-header__subtitle {
  font-family: var(--font-display);
  font-size: var(--text-xl);
  font-weight: 400;
  font-style: italic;
  color: var(--muted2);
  margin-bottom: var(--space-3);
}

.entity-header__tags {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  margin-top: var(--space-2);
}

/* ── Breadcrumb ──────────────────────────────────────────── */
.breadcrumb {
  display: flex;
  align-items: center;
  gap: 6px;
  margin-bottom: var(--space-4);
  font-family: var(--font-body);
  font-size: var(--text-xs);
  flex-wrap: wrap;
}

.breadcrumb__link {
  color: var(--muted);
  text-decoration: none;
  transition: color var(--transition-fast);
}

.breadcrumb__link:hover {
  color: var(--ink);
}

.breadcrumb__sep {
  color: var(--border);
  user-select: none;
}

.breadcrumb__current {
  color: var(--ink);
  font-weight: 500;
}

/* ── Data table (object/feature fields) ──────────────────── */
.data-table {
  width: 100%;
  border-collapse: collapse;
  border: 1px solid var(--border);
  margin: var(--space-4) 0;
}

.data-table tr {
  border-bottom: 1px solid var(--border2);
}

.data-table tr:last-child {
  border-bottom: none;
}

.data-table th {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  color: var(--muted);
  background: var(--parchment);
  padding: 9px 16px;
  text-align: left;
  width: 200px;
  vertical-align: top;
  border-right: 1px solid var(--border2);
  white-space: nowrap;
}

.data-table td {
  font-family: var(--font-body);
  font-size: var(--text-sm);
  color: var(--ink);
  padding: 9px 16px;
  vertical-align: top;
  line-height: 1.5;
}

.data-table td .certainty-badge {
  margin-right: 6px;
}

/* ── Scientific section headers ──────────────────────────── */
.sci-section {
  margin-top: var(--space-8);
  margin-bottom: var(--space-4);
}

.sci-section__number {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--muted);
  margin-bottom: 4px;
}

.sci-section__heading {
  font-family: var(--font-display);
  font-size: var(--text-2xl);
  font-weight: 400;
  color: var(--ink);
  padding-bottom: var(--space-2);
  border-bottom: 1px solid var(--border);
}

/* ── Abstract block ──────────────────────────────────────── */
.abstract-block {
  background: var(--parchment);
  border: 1px solid var(--border);
  border-left: 3px solid var(--gold);
  padding: var(--space-4);
  margin: var(--space-5) 0;
}

.abstract-block__label {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--muted);
  margin-bottom: var(--space-2);
}

.abstract-block__text {
  font-family: var(--font-body);
  font-size: var(--text-md);
  font-weight: 300;
  line-height: 1.7;
  color: var(--muted2);
  max-width: 62ch;
}

/* ── Body text within templates ──────────────────────────── */
.body-text {
  font-family: var(--font-body);
  font-size: var(--text-base);
  line-height: 1.75;
  color: var(--muted2);
  max-width: 66ch;
}

.body-text p + p {
  margin-top: var(--space-3);
}

.body-text strong {
  color: var(--ink);
  font-weight: 600;
}

.body-text em {
  font-style: italic;
}

/* ── Figure block ────────────────────────────────────────── */
.figure-block {
  margin: var(--space-6) 0;
}

.figure-block__image {
  display: block;
  width: 100%;
  border: 1px solid var(--border);
  background: var(--parchment2);
}

.figure-block__caption {
  display: grid;
  grid-template-columns: auto 1fr;
  gap: var(--space-2);
  margin-top: var(--space-2);
  padding-top: var(--space-2);
  border-top: 1px solid var(--border2);
}

.figure-block__fig-num {
  font-family: var(--font-display);
  font-size: var(--text-sm);
  font-weight: 600;
  color: var(--muted2);
  white-space: nowrap;
}

.figure-block__fig-text {
  font-family: var(--font-body);
  font-size: var(--text-sm);
  color: var(--muted);
  line-height: 1.5;
}

/* ── Image gallery grid ──────────────────────────────────── */
.gallery-grid {
  display: grid;
  gap: var(--space-2);
  margin: var(--space-4) 0;
}

.gallery-grid--2 { grid-template-columns: 1fr 1fr; }
.gallery-grid--3 { grid-template-columns: 1fr 1fr 1fr; }
.gallery-grid--4 { grid-template-columns: 1fr 1fr 1fr 1fr; }

.gallery-item {
  border: 1px solid var(--border);
  background: var(--parchment2);
  overflow: hidden;
  cursor: pointer;
  transition: border-color var(--transition-medium);
}

.gallery-item:hover {
  border-color: var(--muted);
}

.gallery-item__image {
  width: 100%;
  height: auto;        /* Natural proportions — never crop */
  object-fit: contain;
  display: block;
}

.gallery-item__label {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 500;
  letter-spacing: 0.04em;
  text-transform: uppercase;
  color: var(--muted);
  padding: 6px 10px;
  border-top: 1px solid var(--border2);
  background: var(--parchment);
}

/* ── Microscopy block (scientific images) ────────────────── */
.micro-block {
  border: 1px solid var(--border);
  margin: var(--space-5) 0;
}

.micro-block__image-area {
  background: #0d0d0d;
  position: relative;
  overflow: hidden;
  min-height: 200px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.micro-block__image-area img {
  display: block;
  width: 100%;
}

.micro-block__scale-bar {
  position: absolute;
  bottom: 12px;
  right: 12px;
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 3px;
}

.micro-block__scale-line {
  height: 2px;
  width: 80px;
  background: #ffffff;
}

.micro-block__scale-label {
  font-family: var(--font-body);
  font-size: 11px;
  font-weight: 500;
  color: #ffffff;
  letter-spacing: 0.04em;
}

.micro-block__meta {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 1px;
  background: var(--border);
  border-top: 1px solid var(--border);
}

.micro-block__meta-cell {
  background: var(--parchment);
  padding: 8px 12px;
}

.micro-block__meta-label {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  color: var(--muted);
  margin-bottom: 2px;
}

.micro-block__meta-value {
  font-family: var(--font-body);
  font-size: var(--text-sm);
  color: var(--muted2);
}

/* ── Placeholder image SVG containers ────────────────────── */
.img-placeholder {
  width: 100%;
  display: block;
  background: var(--parchment2);
  border: 1px solid var(--border);
}

.img-placeholder--micro {
  background: #111;
}

/* ── Scientific note block ───────────────────────────────── */
.note-block {
  margin: var(--space-4) 0;
  padding: var(--space-3);
  border: 1px solid var(--border2);
  background: var(--parchment);
  display: grid;
  grid-template-columns: 28px 1fr;
  gap: var(--space-2);
}

.note-block__label {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--muted);
  writing-mode: vertical-lr;
  transform: rotate(180deg);
  align-self: start;
  padding-top: 4px;
}

.note-block__text {
  font-family: var(--font-body);
  font-size: var(--text-sm);
  color: var(--muted2);
  line-height: 1.6;
}

/* ── Warning block ───────────────────────────────────────── */
.warning-block {
  margin: var(--space-4) 0;
  padding: var(--space-3);
  border: 1px solid #8b3a2a;
  background: #fcf5f4;
  display: grid;
  grid-template-columns: auto 1fr;
  gap: var(--space-3);
  align-items: start;
}

.warning-block__label {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: #8b3a2a;
  white-space: nowrap;
}

.warning-block__text {
  font-family: var(--font-body);
  font-size: var(--text-sm);
  color: var(--muted2);
  line-height: 1.6;
}

/* ── Bibliography block ──────────────────────────────────── */
.bibliography-block {
  margin-top: var(--space-10);
  padding-top: var(--space-5);
  border-top: 2px solid var(--border);
}

.bibliography-block__title {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--muted);
  margin-bottom: var(--space-4);
}

.bibliography-entry {
  font-family: var(--font-body);
  font-size: var(--text-sm);
  color: var(--ink);
  line-height: 1.6;
  padding-left: 2em;
  text-indent: -2em;
  margin-bottom: var(--space-2);
}

.bibliography-entry__id {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  color: var(--muted);
  font-weight: 600;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  display: block;
  margin-bottom: 2px;
  text-indent: 0;
}

.bibliography-entry a {
  color: var(--muted2);
  text-decoration: underline;
  text-decoration-color: var(--border);
  transition: color var(--transition-fast);
}

.bibliography-entry a:hover {
  color: var(--ink);
}

/* ── Sidebar components ──────────────────────────────────── */
.sidebar-section {
  border: 1px solid var(--border);
  background: var(--stone);
}

.sidebar-section__header {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--muted);
  padding: var(--space-2) var(--space-3);
  border-bottom: 1px solid var(--border2);
  background: var(--parchment);
}

.sidebar-section__body {
  padding: var(--space-2) 0;
}

.sidebar-link {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  padding: 7px var(--space-3);
  border-bottom: 1px solid var(--border2);
  text-decoration: none;
  transition: background var(--transition-fast);
}

.sidebar-link:last-child {
  border-bottom: none;
}

.sidebar-link:hover {
  background: var(--parchment);
}

.sidebar-link__name {
  font-family: var(--font-body);
  font-size: var(--text-sm);
  color: var(--muted2);
  line-height: 1.3;
}

.sidebar-link__id {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  color: var(--muted);
  font-weight: 600;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  flex-shrink: 0;
  margin-left: var(--space-2);
}

.sidebar-anchor {
  display: block;
  padding: 7px var(--space-3);
  border-bottom: 1px solid var(--border2);
  font-family: var(--font-body);
  font-size: var(--text-sm);
  color: var(--muted2);
  text-decoration: none;
  transition: background var(--transition-fast), color var(--transition-fast);
}

.sidebar-anchor:last-child {
  border-bottom: none;
}

.sidebar-anchor:hover {
  background: var(--parchment);
  color: var(--ink);
}

.sidebar-anchor.active {
  color: var(--gold);
  font-weight: 500;
}

/* ── Comparative table ───────────────────────────────────── */
.compare-table {
  width: 100%;
  border-collapse: collapse;
  border: 1px solid var(--border);
  font-family: var(--font-body);
  font-size: var(--text-sm);
  margin: var(--space-4) 0;
}

.compare-table thead th {
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  color: var(--muted2);
  background: var(--parchment);
  padding: 10px 14px;
  text-align: left;
  border-bottom: 2px solid var(--border);
}

.compare-table thead th:first-child {
  background: var(--parchment2);
  border-right: 2px solid var(--border);
}

.compare-table tbody td {
  padding: 9px 14px;
  border-bottom: 1px solid var(--border2);
  vertical-align: top;
  line-height: 1.5;
  color: var(--ink);
}

.compare-table tbody td:first-child {
  font-weight: 600;
  color: var(--muted2);
  background: var(--parchment);
  border-right: 2px solid var(--border);
  white-space: nowrap;
}

.compare-table tbody tr:nth-child(even) td:not(:first-child) {
  background: #faf8f5;
}

.compare-table .cell--match    { color: #3a6b3a; font-weight: 500; }
.compare-table .cell--absent   { color: var(--muted); font-style: italic; }
.compare-table .cell--uncertain{ color: var(--muted); }
.compare-table .cell--uncertain::before { content: '? '; }

/* ── Workflow steps (methodology) ────────────────────────── */
.workflow {
  display: flex;
  flex-direction: column;
  gap: 1px;
  background: var(--border);
  border: 1px solid var(--border);
  margin: var(--space-4) 0;
}

.workflow-step {
  background: var(--stone);
  display: grid;
  grid-template-columns: 56px 1fr;
  min-height: 64px;
}

.workflow-step__num {
  background: var(--parchment);
  display: flex;
  align-items: center;
  justify-content: center;
  border-right: 1px solid var(--border2);
  font-family: var(--font-display);
  font-size: var(--text-2xl);
  font-weight: 300;
  color: var(--border);
}

.workflow-step__content {
  padding: var(--space-2) var(--space-3);
}

.workflow-step__title {
  font-family: var(--font-body);
  font-size: var(--text-sm);
  font-weight: 600;
  color: var(--muted2);
  margin-bottom: 4px;
  letter-spacing: 0.02em;
}

.workflow-step__desc {
  font-family: var(--font-body);
  font-size: var(--text-sm);
  color: var(--muted);
  line-height: 1.55;
}

/* ── Timeline (dynasty) ──────────────────────────────────── */
.dynasty-timeline {
  position: relative;
  padding-left: 32px;
  margin: var(--space-4) 0;
}

.dynasty-timeline::before {
  content: '';
  position: absolute;
  left: 12px;
  top: 0;
  bottom: 0;
  width: 1px;
  background: var(--border);
}

.timeline-period {
  position: relative;
  padding-bottom: var(--space-4);
}

.timeline-period::before {
  content: '';
  position: absolute;
  left: -24px;
  top: 6px;
  width: 8px;
  height: 8px;
  border: 1px solid var(--border);
  background: var(--parchment);
}

.timeline-period.active::before {
  background: var(--gold);
  border-color: var(--gold);
}

.timeline-period__dates {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.06em;
  color: var(--muted);
  margin-bottom: 2px;
}

.timeline-period__name {
  font-family: var(--font-display);
  font-size: var(--text-xl);
  font-weight: 400;
  color: var(--ink);
  line-height: 1.2;
  margin-bottom: 4px;
}

.timeline-period__note {
  font-family: var(--font-body);
  font-size: var(--text-sm);
  color: var(--muted);
  line-height: 1.5;
}

/* ── Kiln map placeholder ────────────────────────────────── */
.kiln-map {
  border: 1px solid var(--border);
  background: var(--parchment2);
  margin: var(--space-4) 0;
  position: relative;
}

.kiln-map__label {
  position: absolute;
  top: var(--space-2);
  left: var(--space-2);
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  color: var(--muted);
  background: var(--stone);
  padding: 2px 8px;
  border: 1px solid var(--border2);
}

/* ── Author profile elements ─────────────────────────────── */
.author-identity {
  display: grid;
  grid-template-columns: 80px 1fr;
  gap: var(--space-4);
  align-items: start;
  padding: var(--space-4);
  border: 1px solid var(--border);
  background: var(--parchment);
  margin-bottom: var(--space-5);
}

.author-avatar {
  width: 80px;
  height: 80px;
  background: var(--parchment2);
  border: 1px solid var(--border);
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--font-display);
  font-size: var(--text-2xl);
  font-weight: 300;
  color: var(--border);
}

.author-name {
  font-family: var(--font-display);
  font-size: var(--text-3xl);
  font-weight: 300;
  color: var(--ink);
  line-height: 1.1;
  margin-bottom: 6px;
}

.author-role {
  font-family: var(--font-body);
  font-size: var(--text-sm);
  color: var(--muted);
  line-height: 1.5;
}

.author-identifiers {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-2);
  margin-top: var(--space-2);
}

.author-id-badge {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  color: var(--muted2);
  background: var(--stone);
  border: 1px solid var(--border);
  padding: 3px 10px;
  text-decoration: none;
  transition: border-color var(--transition-fast);
}

.author-id-badge:hover {
  border-color: var(--muted);
}

/* ── Publication record (article template) ───────────────── */
.pub-meta-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1px;
  background: var(--border);
  border: 1px solid var(--border);
  margin: var(--space-4) 0;
}

.pub-meta-cell {
  background: var(--stone);
  padding: var(--space-2) var(--space-3);
}

.pub-meta-cell__label {
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  color: var(--muted);
  margin-bottom: 3px;
}

.pub-meta-cell__value {
  font-size: var(--text-sm);
  color: var(--ink);
  line-height: 1.4;
}

/* ── DOI / external link block ───────────────────────────── */
.doi-block {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  padding: var(--space-3);
  border: 1px solid var(--border2);
  background: var(--parchment);
  margin: var(--space-4) 0;
}

.doi-block__label {
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--muted);
  flex-shrink: 0;
}

.doi-block__value {
  font-family: var(--font-body);
  font-size: var(--text-sm);
  color: var(--muted2);
  font-style: italic;
}

/* ── Keywords strip ──────────────────────────────────────── */
.keywords-strip {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  margin: var(--space-3) 0;
}

.keyword {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 500;
  letter-spacing: 0.04em;
  text-transform: uppercase;
  color: var(--muted2);
  background: var(--parchment);
  border: 1px solid var(--border);
  padding: 2px 8px;
}

/* ── Diagnostic criteria grid ────────────────────────────── */
.criteria-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1px;
  background: var(--border);
  border: 1px solid var(--border);
  margin: var(--space-4) 0;
}

.criteria-cell {
  background: var(--stone);
  padding: var(--space-3);
}

.criteria-cell__level {
  font-size: var(--text-xs);
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--muted);
  margin-bottom: 6px;
  padding-bottom: 6px;
  border-bottom: 1px solid var(--border2);
}

.criteria-cell__content {
  font-size: var(--text-sm);
  color: var(--muted2);
  line-height: 1.6;
}

/* ── Formation mechanism block ───────────────────────────── */
.mechanism-block {
  display: grid;
  grid-template-columns: 1fr 240px;
  gap: var(--space-5);
  align-items: start;
  margin: var(--space-5) 0;
}

/* ── Domain accent strip ─────────────────────────────────── */
.domain-strip {
  height: 3px;
  width: 100%;
  margin-bottom: var(--space-4);
}

.domain-strip--objects   { background: var(--gold); }
.domain-strip--features  { background: var(--muted2); }
.domain-strip--kilns     { background: var(--muted); }
.domain-strip--dynasties { background: var(--ming-yel); }
.domain-strip--method    { background: var(--ru-blue); }
.domain-strip--pubs      { background: var(--qing-cob); }
.domain-strip--authors   { background: var(--border); }

/* ── Page-level tag system ───────────────────────────────── */
.tag {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 500;
  letter-spacing: 0.04em;
  text-transform: uppercase;
  color: var(--muted2);
  background: var(--parchment);
  border: 1px solid var(--border);
  padding: 2px 8px;
  display: inline-block;
}

.certainty-badge {
  font-family: var(--font-body);
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  padding: 2px 6px;
  border: 1px solid currentColor;
  display: inline-block;
}

.certainty-badge--confirmed { color: #3a6b3a; }
.certainty-badge--probable  { color: var(--qing-cob); }
.certainty-badge--proposed  { color: var(--ming-yel); }
.certainty-badge--disputed  { color: #8b3a2a; }

/* ── Pagination ──────────────────────────────────────────── */
.pagination {
  display: flex;
  align-items: center;
  gap: var(--space-1);
  margin-top: var(--space-6);
  padding-top: var(--space-4);
  border-top: 1px solid var(--border);
}

.pagination__item {
  padding: 6px 12px;
  border: 1px solid var(--border);
  color: var(--muted2);
  text-decoration: none;
  font-size: var(--text-sm);
  transition: background var(--transition-fast);
}

.pagination__item:hover     { background: var(--parchment); }
.pagination__item.active    { background: var(--ink); color: var(--stone); border-color: var(--ink); }
.pagination__item.disabled  { color: var(--border); pointer-events: none; }

/* ── Responsive: entity page ─────────────────────────────── */
@media (max-width: 1100px) {
  .entity-page {
    grid-template-columns: 1fr;
  }
  .entity-page__sidebar {
    position: static;
    max-height: none;
    order: 2;
  }
  .entity-page__main {
    order: 1;
  }
  .mechanism-block {
    grid-template-columns: 1fr;
  }
  .pub-meta-grid {
    grid-template-columns: 1fr;
  }
  .micro-block__meta {
    grid-template-columns: 1fr 1fr;
  }
}

@media (max-width: 768px) {
  .entity-header__title {
    font-size: var(--text-3xl);
  }
  .data-table th {
    width: 120px;
    font-size: 10px;
  }
  .gallery-grid--3,
  .gallery-grid--4 {
    grid-template-columns: 1fr 1fr;
  }
  .criteria-grid {
    grid-template-columns: 1fr;
  }
  .author-identity {
    grid-template-columns: 1fr;
  }
  .compare-table {
    display: block;
    overflow-x: auto;
    min-width: 500px;
  }
  .workflow-step {
    grid-template-columns: 40px 1fr;
  }
}

@media (max-width: 480px) {
  .gallery-grid--2,
  .gallery-grid--3,
  .gallery-grid--4 {
    grid-template-columns: 1fr;
  }
  .micro-block__meta {
    grid-template-columns: 1fr;
  }
}
